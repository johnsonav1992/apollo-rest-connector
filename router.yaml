connectors:
  preview_connect_v0_3: true
  # sources:
  #   subgraph.source_name:
  #     $config:
  #       apiKey: ${env.API_KEY}

cors:
  policies:
    - origins:
        - http://localhost:3000
      allow_credentials: true